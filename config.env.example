# Authentication Configuration
# Choose ONE authentication method: GitHub App (recommended) or PAT

# Option 1: GitHub App (recommended for better security and auditability)
# Create at: https://github.com/organizations/YOUR_ORG/settings/apps/new
GITHUB_APP_ID=
GITHUB_APP_INSTALLATION_ID=
GITHUB_APP_PRIVATE_KEY_PATH=

# Option 2: Personal Access Token (classic)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for repo-level) or admin:org (for org-level)
# GITHUB_PAT=

# Target Configuration (REQUIRED)
GITHUB_ORG=your-org                    # Your GitHub organization or username
GITHUB_REPO=                           # Leave empty for org-level runners, or specify repo name for repo-level

# Runner Configuration (REQUIRED)
RUNNER_NAMESPACE=arc-runners           # Kubernetes namespace for runner resources
RUNNER_SCALE_SET_NAME=my-runners       # Name used in workflow runs-on field
MIN_RUNNERS=0                          # Minimum runners (0 = scale to zero when idle)
MAX_RUNNERS=32                         # Maximum concurrent runners

# Cluster Configuration (OPTIONAL)
K3D_CLUSTER_NAME=arc-cluster           # Name of the k3d Kubernetes cluster

# Performance Optimization (OPTIONAL)
ENABLE_CACHE_PROXY=true                # Enable caching proxies for Docker registry, apt, and HTTP
                                       # Set to false to disable (slightly faster startup, no caching benefits)

# Advanced: Custom Runner Image (OPTIONAL)
# Uncomment to override the default runner image name
# Only needed if you want a different image tag
# RUNNER_IMAGE=plasma-runner:latest
